import { Typography } from 'antd';

const { Title } = Typography;

const TagPage: React.FC = () => {
  return (
    <div>
      <Title level={3}>标签</Title>
      <p>开发中...</p>
    </div>
  );
};

export default TagPage;
